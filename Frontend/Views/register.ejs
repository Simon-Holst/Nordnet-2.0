<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Make account</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: #181818;
            font-family: Arial, sans-serif;
            margin: 0;
        }
        #registerContainer {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
        }
        #registerForm {
            background: linear-gradient(to right, #4B3C8E, #7A5EC8);
            color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 300px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        h3 {
            margin-bottom: 15px;
        }
        input {
            width: 100%;
            padding: 10px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }
        button {
            background: #181818;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            margin-top: 10px;
        }
        button:hover {
            background: #242424;
        }
        p {
            margin-top: 10px;
        }
        a {
            color: #6e8efb;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        h1{
            color: white;
            text-align: center;
            margin-bottom: 60px;
            font-size: 50px;
        }
        .page-header {
            display: flex;
            align-items: baseline; 
            justify-content: center;
            gap: 12px;
            margin-top: 20px;
            margin-bottom: 40px;
        }
        .page-header h1 {
            font-size: 50px;
            margin: 0;
            line-height: 1;
        }
        .header-logo {
            height: 48px; 
            width: auto;
            transform: translateY(4px); 
        }
    </style>
</head>

<div class="page-header">
    <h1>PortfolioTracker</h1>
    <img src="/Pig-logo.png" alt="Pig Bank Logo" class="header-logo">
</div>

<body>
    <form id="registerForm">
        <h3>Create new account</h3>
        <label for="email">E-mail:</label>
        <input type="email" id="email" name="email" required><br><br>

        <label for="username">Username:</label>
        <input type="text" id="username" name="username" required><br><br>

        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required><br><br>

        <button type="submit">Register account</button>
        <p>All ready have an account? <a href="/">Log in here</a></p>
    </form>

    <script type="module">
        import { handleRegister } from '/js/auth.js';
        document.getElementById('registerForm').addEventListener('submit', handleRegister);
    </script>
    
</body>
</html>